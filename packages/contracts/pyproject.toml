[project]
name = "contracts"
version = "0.1.0"
description = "Wspoldzielone schematy danych (Pydantic) — zrodlo prawdy."
requires-python = ">=3.12"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dt_contracts"]
